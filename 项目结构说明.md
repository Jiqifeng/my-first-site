# 项目文件结构说明（新手向）

本文说明 **my-first-site** 仓库里主要文件和文件夹是干什么的、它们怎么配合工作。读完你会知道「改哪里会动到页面」「哪些是配置、不用天天改」。

---

## 一、这是个什么项目？

这是一个用 **Next.js 15** 做的网站前端项目：多产品展示页、苹果风极简界面、支持深色模式，并用了 **Tailwind CSS** 写样式、**Framer Motion** 做动效。**TypeScript** 用来在写代码时多一层类型检查，减少低级错误。

---

## 二、根目录一览（树状结构）

```
my-first-site/
├── .gitignore              # 告诉 Git 不要提交哪些文件
├── README.md               # 项目简介与运行方式（给人类看的说明）
├── eslint.config.mjs       # 代码风格与质量检查（ESLint）配置
├── next.config.ts          # Next.js 框架配置
├── package.json            # 项目名、脚本命令、依赖列表
├── package-lock.json       # 锁定依赖的确切版本（保证大家装的一样）
├── postcss.config.mjs      # PostCSS：把 Tailwind 等接到构建流程里
├── tailwind.config.ts      # Tailwind：主题色、字体、扫描哪些文件里的 class
├── tsconfig.json           # TypeScript 编译器选项
├── src/                    # 几乎所有你自己写的页面和组件都在这里
│   ├── app/                # Next.js「App Router」：路由 + 页面入口
│   └── components/         # 可复用的 UI 组件
└── （运行 npm install 后会出现 node_modules/，已被 .gitignore 忽略）
```

> **说明**：若你以后添加 **`public/`** 文件夹，里面的图片、字体等会按「原样网址」访问，例如 `public/logo.png` 对应 `http://localhost:3000/logo.png`。产品 A 详情页注释里提到的本地图就适合放在 `public/product-a/` 下。

---

## 三、根目录各文件作用（逐个说明）

### `package.json`

- **作用**：项目的「说明书 + 依赖清单」。
- **常见字段**：
  - **`scripts`**：你在终端里运行的命令，例如 `npm run dev` 启动开发服务器。
  - **`dependencies`**：网站运行需要的库（如 `next`、`react`）。
  - **`devDependencies`**：只在开发时用的工具（如 `typescript`、`eslint`）。
- **新手要点**：换电脑或克隆项目后，先在项目根目录执行 `npm install`，会根据 `package.json`（和 `package-lock.json`）安装依赖。

### `package-lock.json`

- **作用**：记录**每一次**安装的依赖的**精确版本**，避免「我电脑上能跑、你电脑上报错」。
- **新手要点**：一般随 `package.json` 一起提交到 Git；不要手改，由 `npm install` 自动维护。

### `next.config.ts`

- **作用**：Next.js 的全局配置，例如图片域名白名单、重定向等。
- **本项目里**：允许从 `images.unsplash.com` 加载远程图片（配合 `next/image` 使用）。

### `tsconfig.json`

- **作用**：告诉 TypeScript 怎么检查你的 `.ts` / `.tsx` 文件。
- **本项目里**：例如 `@/*` 映射到 `./src/*`，所以代码里可以写 `import x from "@/components/Header"`，不用写一长串相对路径。

### `tailwind.config.ts`

- **作用**：Tailwind 的配置：在哪些文件里扫描 `className`、深色模式策略、自定义颜色/字体等。
- **本项目里**：`darkMode: "class"` 表示用给 `<html>` 加 class 的方式切换深色；`surface`、`ink` 等颜色来自 CSS 变量（在 `globals.css` 里定义）。

### `postcss.config.mjs`

- **作用**：构建时处理 CSS 的管道；这里接入了 **Tailwind** 和 **Autoprefixer**（自动补浏览器前缀）。
- **新手要点**：一般不用改，除非你要加新的 CSS 后处理插件。

### `eslint.config.mjs`

- **作用**：**ESLint** 的规则配置，执行 `npm run lint` 时会用。帮助发现常见问题和不符合 Next.js 推荐写法的代码。
- **新手要点**：报错时先看提示改代码；规则可以慢慢熟悉，不必一次背完。

### `.gitignore`

- **作用**：列出**不要**交给 Git 跟踪的文件（例如 `node_modules`、`.next` 构建缓存、本地环境文件等）。
- **新手要点**：敏感信息（密钥、本地 `.env`）永远不要提交；依赖和构建产物通常也不提交。

### `README.md`

- **作用**：项目概览、技术栈、如何运行。面向读仓库的人（包括未来的你）。
- **与本文关系**：README 偏「快速上手」；`项目结构说明.md` 偏「每个文件干什么」。

---

## 四、`src/app/` —— 页面与路由（App Router）

Next.js 约定：**文件夹结构 ≈ 网址路径**。

| 路径（磁盘） | 在浏览器里大致对应 | 说明 |
|--------------|-------------------|------|
| `src/app/page.tsx` | 网站首页 `/` | 首页内容：Hero + 产品 A/B/C 区块 |
| `src/app/layout.tsx` | 包裹所有页面的「外壳」 | 全站共同的 `<html>`、`<body>`、顶栏 `Header`、主题 `ThemeProvider` |
| `src/app/globals.css` | 全局样式 | 全站 CSS 变量、Tailwind 的 `@tailwind` 指令、基础样式 |
| `src/app/product-a/page.tsx` | 子页面 `/product-a` | 产品 A 的详情页（独立路由） |

**新手要点**：

- 想**改首页长什么样**：主要看 `page.tsx` 和它引用的组件。
- 想**改全站标题、顶栏、深色模式包裹方式**：看 `layout.tsx`。
- 想**改浅色/深色下的背景色、文字色**：看 `globals.css` 和 `tailwind.config.ts` 里和 CSS 变量相关的部分。
- **新建一个页面**：例如在 `src/app/about/page.tsx` 里写组件，就会自动多一个 `/about` 地址（需自己创建文件夹和文件）。

---

## 五、`src/components/` —— 可复用组件

把界面拆成小块，方便维护和复用。

| 文件 / 文件夹 | 作用（通俗说） |
|---------------|----------------|
| `Header.tsx` | 顶部导航栏（玻璃效果等） |
| `Hero.tsx` | 首页大标题区域（产品名、Slogan、按钮、入场动效） |
| `ThemeProvider.tsx` | 包裹应用，提供深色/浅色主题切换能力（常和 `localStorage` 或 class 配合） |
| `products/ProductA.tsx` | 首页上「产品 A」那一整块展示 |
| `products/ProductB.tsx` | 产品 B 区块（当前可能是占位） |
| `products/ProductC.tsx` | 产品 C 区块（当前可能是占位） |

**新手要点**：页面文件（`page.tsx`）负责「拼积木」；具体长什么样、动画在哪，往往在对应的 `components` 里改。

---

## 六、常用终端命令（和文件的关系）

这些命令定义在 `package.json` 的 `scripts` 里：

| 命令 | 作用 |
|------|------|
| `npm install` | 根据 `package.json` / `package-lock.json` 安装依赖，生成 `node_modules` |
| `npm run dev` | 启动开发服务器（带热更新），浏览器访问 `http://localhost:3000` |
| `npm run build` | 生产构建，生成优化后的产物（输出在 `.next/`，已被忽略） |
| `npm start` | 在**已经 build 过**的前提下，启动生产模式服务器 |
| `npm run lint` | 运行 ESLint，检查代码规范 |

---

## 七、数据流小结（帮助建立整体印象）

1. 用户打开某个网址 → Next.js 根据 **`src/app/` 下的路由**找到对应的 `page.tsx`。
2. `layout.tsx` 先包一层（全局样式、Header、主题）。
3. `page.tsx` 里组装 **`components`**，展示具体内容。
4. 样式来自 **`className` + Tailwind**，全局变量在 **`globals.css`**，Tailwind 主题在 **`tailwind.config.ts`**。
5. 图片若用 **`next/image`** 且是外站图，域名要在 **`next.config.ts`** 里允许。

---

## 八、你接下来可以怎么练手？

- 改 **`src/app/page.tsx`**：调整首页上各区块的顺序或是否显示。
- 改 **`src/components/Hero.tsx`**：改文案、按钮链接。
- 在 **`src/app/`** 下新建一个文件夹 + `page.tsx`：练习新增一个路由页面。

若某文件在本文没提到，多半是后来新增的；你可以用编辑器全局搜索文件名，看它被谁 `import`，就能猜到作用。

---

*文档与当前仓库结构一致；若你移动或重命名了文件，请以实际代码为准。*
