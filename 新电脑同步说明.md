# 新电脑 · Cursor 项目同步说明

换电脑后，按本说明可在 Cursor 中快速恢复 **my-first-site** 开发环境。

---

## 同步原则（一句话）

| 内容 | 方式 |
|------|------|
| 代码 | GitHub |
| 依赖 | 新电脑 `npm install` |
| 密钥与环境变量 | 手动拷贝 **`.env.local`**（U 盘 / 密码管理器） |
| 数据库、图片 | 已在云端（Neon、Cloudinary），无需拷文件 |

---

## 1. 新电脑前置条件

安装：

- [Node.js](https://nodejs.org/) 20+（自带 npm）
- [Git](https://git-scm.com/)
- [Cursor](https://cursor.com/)

验证：

```bash
node -v
npm -v
git -v
```

---

## 2. 拉取代码

```bash
git clone https://github.com/Jiqifeng/my-first-site.git
cd my-first-site
```

在 Cursor 中：**File → Open Folder**，选择 `my-first-site` 目录。

---

## 3. 恢复环境变量（唯一需手动带的文件）

将旧电脑项目根目录下的 **`.env.local`** 复制到新电脑**同一位置**：

```text
my-first-site/
├── .env.local      ← 放这里（与 package.json 同级）
├── .env.example    ← 模板，已在 GitHub 上
├── package.json
└── ...
```

若找不到旧文件，可从 Vercel / Neon / Cloudinary 控制台对照填写，或：

```bash
cp .env.example .env.local
# 再编辑 .env.local 填入真实值
```

**不要**把 `.env.local` 提交到 GitHub。

---

## 4. 安装依赖并启动

```bash
npm install
npm run prisma:generate
npm run dev
```

浏览器打开：<http://localhost:3000>

若数据库 schema 有更新（少见，换电脑通常不需要）：

```bash
npm run prisma:migrate
```

---

## 5. 配置 Git 推送（新电脑做一次）

仓库远程：`https://github.com/Jiqifeng/my-first-site.git`

首次 `git push` 时按提示登录 GitHub，或使用 **Personal Access Token**（密码处粘贴 Token，不是 GitHub 登录密码）。

macOS 建议启用钥匙串记住凭证：

```bash
git config --global credential.helper osxkeychain
```

---

## 6. 日常开发流程

```bash
# 开始工作前
git pull origin main

# 改完代码
git add .
git commit -m "说明本次改动"
git push origin main
```

路径含方括号时请加引号，例如：

```bash
git add "src/app/image/[id]/page.tsx"
```

推送到 `main` 后，Vercel 会自动部署（若项目已连接）。

---

## 7. 不要同步这些内容

| 目录/文件 | 原因 |
|-----------|------|
| `node_modules/` | 体积大，与系统相关，用 `npm install` 重建 |
| `.next/` | 构建缓存，本地自动生成 |
| `.env.local` | 含密钥，走 U 盘/密码管理器，不进 Git |
| `.vercel/` | 本地 Vercel 链接信息，可选忽略 |

---

## 8. 给 Cursor 的上下文（可选）

在新电脑 Cursor 对话中可直接说明：

> 这是 Gallery Hub 项目，Next.js 15 + Prisma + NextAuth + Cloudinary。  
> 代码已从 GitHub clone，`.env.local` 已就位。请帮我检查本地能否 `npm run dev` 正常启动。

项目规则（若已提交）：`.cursor/rules/` 会随仓库一起 clone。

更完整的部署与验收说明见根目录 **`README.md`**。

---

## 9. 验收清单

- [ ] `npm run dev` 能启动，无报错
- [ ] 访问 `/login` 页面正常
- [ ] `git pull` / `git push` 成功
- [ ] GitHub 上能看到最新提交
