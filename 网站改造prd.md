# 网站改造 PRD（第一期）

## 1. 文档信息

- 文档名称：网站改造 PRD（第一期）
- 文件名：`网站改造prd`
- 项目名称：my-first-site
- 当前阶段：MVP（第一期）
- 语言范围：中文
- 部署目标：Vercel

---

## 2. 项目背景

当前网站为产品介绍型站点，页面结构以产品宣传为主，缺少用户体系、权限控制与内容管理能力，无法支撑“会员访问 + 内容持续运营”的业务目标。

本次改造目标是将网站升级为“登录后可访问的图片展示平台”，并完成基础用户管理和后台内容管理闭环，为第二期视频能力和互动能力预留扩展空间。

---

## 3. 产品目标

### 3.1 业务目标

1. 将网站定位从“产品介绍”转为“图片内容展示平台”。
2. 建立可控的用户准入机制（邀请码注册 + 登录访问）。
3. 建立管理员可用的内容发布能力（上传、发布、上下架）。
4. 完成可上线、可维护、可扩展的第一期版本。

### 3.2 非目标（第一期不做）

1. 不支持视频上传与播放（后续版本）。
2. 不支持第三方登录（如微信/Google/GitHub）。
3. 不支持评论、点赞、收藏、关注等互动功能。
4. 不支持多语言（仅中文）。
5. 不支持付费会员分级。

---

## 4. 用户与角色

### 4.1 用户角色定义

- 游客（未登录用户）
  - 可访问：登录页、注册页
  - 不可访问：图片内容页、详情页、任何后台页

- 注册用户（普通用户）
  - 可访问：登录后首页、图片详情、个人中心
  - 不可访问：后台管理页面（上传、邀请码管理）

- 管理员（admin）
  - 可访问：普通用户全部页面
  - 额外可访问：上传管理、图片管理、邀请码管理

### 4.2 用户来源

- 本期注册必须通过邀请码
- 邀请码策略：一次性使用（用后失效）

---

## 5. 核心需求范围（第一期）

### 5.1 账号与认证

1. 支持邮箱注册
2. 支持邮箱 + 密码登录
3. 支持退出登录
4. 注册必须填写有效邀请码
5. 邀请码成功注册后立即失效
6. 未登录访问受保护路由时自动跳转登录页

### 5.2 图片内容展示

1. 登录后首页展示已发布图片流
2. 支持进入图片详情页查看大图和基础信息
3. 仅展示状态为 `published` 的内容

### 5.3 管理员后台（仅 admin）

1. 图片上传（直传云存储）
2. 图片基础信息录入（标题、描述）
3. 图片状态管理：`draft` / `published` / `archived`
4. 邀请码管理（生成、查看状态）

### 5.4 视觉与体验改造

1. 从“产品宣传版式”调整为“极简画廊风”
2. 首页采用图片流/网格展示
3. 保留简洁、轻动效、响应式适配
4. 全站中文文案统一

---

## 6. 功能清单与优先级

### P0（上线必需）

1. 注册（邀请码校验）
2. 登录/退出
3. 路由鉴权（未登录拦截）
4. 管理员上传图片
5. 图片发布与展示（登录后可见）
6. 管理员邀请码生成与查看

### P1（本期建议，如时间允许）

1. 管理端图片列表筛选（按状态）
2. 邀请码设置过期时间
3. 基础分页能力

### P2（后续版本）

1. 视频上传与播放
2. 互动功能（点赞/评论/收藏）
3. 搜索推荐与个性化排序

---

## 7. 用户流程（关键路径）

### 7.1 注册流程（邀请码）

1. 用户进入注册页，填写邮箱、密码、邀请码
2. 系统校验邀请码有效性（存在、未使用、未过期）
3. 校验通过后创建用户
4. 标记邀请码为已使用
5. 注册成功后跳转登录页或自动登录

### 7.2 登录访问流程

1. 用户在登录页提交邮箱和密码
2. 登录成功后进入首页
3. 访问受保护页面时检查会话状态
4. 未登录用户统一重定向到登录页

### 7.3 管理员发布流程

1. 管理员进入上传页
2. 获取上传签名并将图片直传存储服务
3. 提交图片元数据入库（标题、描述、URL、状态）
4. 设置为 `published` 后，普通登录用户可见

---

## 8. 信息架构与路由建议

### 8.1 公开路由

- `/login`
- `/register`

### 8.2 登录后路由

- `/`：图片流首页
- `/image/[id]`：图片详情页
- `/profile`：个人中心

### 8.3 管理员路由

- `/admin/upload`
- `/admin/images`
- `/admin/invites`

---

## 9. 数据模型（MVP）

### 9.1 User

- `id`
- `email`（唯一）
- `passwordHash`
- `role`（`admin` / `user`）
- `createdAt`

### 9.2 InviteCode

- `id`
- `code`（唯一）
- `expiresAt`（可空）
- `usedAt`（可空）
- `usedByUserId`（可空）
- `createdBy`
- `enabled`（是否启用）

### 9.3 ImageItem

- `id`
- `title`
- `description`（可空）
- `imageUrl`
- `publicId`（云存储资源标识）
- `status`（`draft` / `published` / `archived`）
- `uploadedBy`
- `createdAt`
- `publishedAt`（可空）

---

## 10. 技术方案

### 10.1 技术栈建议

- 前端/全栈框架：Next.js（App Router）
- 样式系统：Tailwind CSS
- 认证方案：Auth.js（邮箱密码）
- ORM：Prisma
- 数据库：PostgreSQL（建议 Neon）
- 图片存储：Cloudinary（浏览器直传 + 服务端签名）
- 部署：Vercel

### 10.2 关键技术原则

1. 媒体文件不经过 Vercel 中转，采用直传方案
2. 页面层和 API 层均做权限校验（双层保护）
3. 所有受保护页面通过中间件统一拦截
4. 为视频能力预留字段与模型扩展路径

---

## 11. 安全与合规要求

1. 密码必须加密存储（不可明文）
2. 会话信息安全存储，关键密钥使用环境变量管理
3. 邀请码不可重复使用（一次性）
4. 管理员接口必须校验角色权限
5. 上传内容仅允许图片类型并限制大小

---

## 12. 设计规范（第一期）

### 12.1 视觉方向

- 主风格：极简画廊风
- 关键词：留白、弱干扰、内容优先、轻动效
- 色彩建议：中性色为主，强调色仅用于交互反馈

### 12.2 页面体验要点

1. 首页优先展示图片内容，不出现产品营销文案
2. 卡片信息简洁（标题 + 时间）
3. 导航结构简化（发现、个人、管理）
4. 移动端优先保证浏览流畅与触控友好

---

## 13. 验收标准（Definition of Done）

### 13.1 功能验收

1. 未登录用户访问内容页会被跳转到登录页
2. 注册必须使用有效邀请码，且邀请码仅可使用一次
3. 普通用户无法访问任何 `/admin/*` 路由
4. 管理员可上传图片并设置发布状态
5. 普通登录用户仅可看到 `published` 内容
6. 首页与详情页可稳定展示图片内容

### 13.2 质量验收

1. 核心页面在桌面端与移动端可用
2. 关键流程（注册/登录/上传/发布）可完整跑通
3. 线上环境变量正确配置，部署成功

---

## 14. 风险与应对

1. 上传链路复杂度风险
   - 应对：采用成熟服务（Cloudinary）+ 标准签名流程

2. 权限遗漏风险
   - 应对：页面中间件 + API 鉴权双层检查 + 冒烟测试

3. 工期膨胀风险
   - 应对：严格控制第一期范围，不提前引入视频与互动

4. 运维配置风险
   - 应对：先在 Vercel Preview 环境验收，再发布正式环境

---

## 15. 里程碑计划（建议）

1. M1（D1-D2）：认证与邀请码闭环完成
2. M2（D3-D4）：管理员上传与内容展示闭环完成
3. M3（D5-D6）：样式重构与管理功能补齐
4. M4（D7）：联调、验收、Vercel 上线

---

## 16. 第二期预研方向（非本期范围）

1. 视频上传与播放
2. 内容互动体系（点赞、评论、收藏）
3. 搜索筛选与推荐排序
4. 运营后台统计看板

---

## 17. 结论

本 PRD 将第一期目标定义为“可上线的邀请制图片会员站”。在不引入视频和复杂互动的前提下，优先完成账号体系、权限控制、管理员发布与用户浏览主路径，确保上线稳定性与后续扩展能力。
